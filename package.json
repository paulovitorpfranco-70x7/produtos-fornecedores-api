{
  "name": "produtos-fornecedores-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start:server",
    "start:server": "node backend/src/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
